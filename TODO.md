# TODO: AOSR Generator (AI Co-founder Project)

## 📌 Правила для ИИ-агента (Jules)
1. Бери в работу строго одну задачу (один чекбокс `[ ]`) за раз.
2. После успешного тестирования и завершения задачи меняй `[ ]` на `[x]`.
3. Обязательно учитывай двойную логику базы данных (Postgres Pool + In-Memory массивы), как реализовано в `server.ts`.
4. Секреты (API ключи) бери только из `.env` или пример '.env.example'.

---

## 🚀 Текущий спринт: Парсинг смет и генерация документов

### Шаг 1: Очистка проекта от неиспользуемых ассетов (Рефакторинг)
- [x] Найти физические файлы векторов 1, 2, 4, 5, 6, 7 (например, `vector6.png`, `vector7.png` и другие) и удалить их из файловой системы проекта. Вектор 3 (и другие нужные) — оставить.
- [x] Найти во всех React-компонентах (проверить `App.tsx`, `HomeView.tsx` и другие View-компоненты) ссылки, импорты и теги `<img>`, которые ссылались на эти удаленные векторы, и аккуратно вычистить их из кода.
- [x] Убедиться, что после удаления приложение успешно собирается и запускается без ошибок об отсутствии файлов, а оставшаяся верстка не сломалась.

### Шаг 2: Обработка Excel файлов (Backend)
- [x] Настроить multer (или аналог) в `server.ts` для приема загружаемых пользователем Excel-файлов (.xlsx, .xls).
- [x] Написать утилиту с использованием библиотеки `xlsx` для чтения загруженного файла и его очистки (удаление пустых строк/столбцов, конвертация в плоский массив или легкий CSV/JSON для экономии токенов).

### Шаг 3: AI-анализ сметы (AI Engine / Backend)
- [ ] Создать функцию обращения к Gemini API (`@google/genai`).
- [ ] Написать системный промпт для Gemini, который будет принимать очищенные данные из Excel. Задача ИИ: извлечь только строительно-монтажные работы, проигнорировав материалы, коэффициенты и заголовки.
- [ ] Настроить Structured Outputs в Gemini, чтобы ИИ возвращал строгий JSON массив объектов: `{ "workName": string, "quantity": number, "unit": string }`.
- [ ] Создать API-эндпоинт `/api/estimates/parse`, который свяжет Шаг 1 и Шаг 2 (принимает файл, отдает JSON).

### Шаг 4: Генерация документа .docx (Backend)
- [ ] Изучить шаблон `aosr_template.docx` (лежит в корне или в /public) и определить ключи для `docxtemplater`.
- [ ] Написать сервис генерации файла с использованием `docxtemplater` и `pizzip`, который принимает JSON от ИИ и подставляет данные в шаблон.
- [ ] Создать API-эндпоинт `/api/documents/generate`, который принимает финальные данные, генерирует Word-файл и отдает его клиенту для скачивания (через `res.download` или отдачу буфера).

### Шаг 5: Интерактивная таблица редактирования (Frontend)
- [ ] Установить библиотеку для удобной работы с таблицами (рекомендуется `@tanstack/react-table` или `react-data-grid`), чтобы реализовать Excel-подобный интерфейс.
- [ ] Создать компонент `EditableActTable.tsx`, который принимает сырые данные от ИИ (после парсинга сметы) и рендерит их в виде таблицы. Структура колонок должна соответствовать референсу "Таблица актов" (№, Наименование работ, Ед. изм., Кол-во и т.д.).
- [ ] Реализовать режим "Inline Editing" (редактирование в ячейках): при клике на ячейку она должна превращаться в `input`, чтобы пользователь мог на лету править текст, объемы или даты.
- [ ] Реализовать локальный стейт-менеджмент: все изменения в ячейках должны мгновенно обновлять общий React-state таблицы.
- [ ] Добавить панель действий под таблицей с кнопкой "Подтвердить данные". По нажатию этой кнопки финальный массив данных отправляется на бэкенд для генерации документов.

### Шаг 6: Генерация PDF и DOCX (Backend)
- [ ] Обновить логику эндпоинта `/api/documents/generate`: он должен принимать отредактированный JSON из нашей интерактивной таблицы.
- [ ] Имплементировать базовую генерацию `.docx` с помощью `docxtemplater` на основе проверенных данных.
- [ ] Добавить функционал конвертации сгенерированного `.docx` в `.pdf` (изучить возможность использования `libreoffice-convert`, `puppeteer` или облачных API для Node.js, так как прямая генерация PDF сложна).
- [ ] Вернуть на фронтенд ссылки на скачивание или бинарные файлы (и `.docx`, и `.pdf`), чтобы пользователь мог выбрать нужный формат.

### Шаг 7: Сохранение прогресса (База данных)
- [ ] Обновить логику добавления акта в массив `acts` внутри структуры проекта в таблице `projects` (поддержать и Postgres, и in-memory).
- [ ] Обновить интерфейс проектов пользователя (`useProjects.ts`), чтобы отображать историю сгенерированных актов со статусами.
---

## 🐛 Баги и исправления (Backlog)
- [ ] *(Jules, записывай сюда найденные ошибки, которые не блокируют текущую задачу, чтобы вернуться к ним позже)*

---

## 💡 Идеи на будущее (Icebox)
- [ ] Поддержка файлов формата `.xml` (Гранд-Смета).
- [ ] Генерация актов входного контроля и журналов КС-6а на основе той же сметы.
- [ ] Дать пользователю возможность скачивать файлы в .docx по отдельности или же собрать их все вместе в один файл .pdf .