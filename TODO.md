# TODO: AOSR Generator (AI Co-founder Project)

## 📌 Правила для ИИ-агента (Jules)
1. Бери в работу строго одну задачу (один чекбокс `[ ]`) за раз.
2. После успешного тестирования и завершения задачи меняй `[ ]` на `[x]`.
3. Обязательно учитывай двойную логику базы данных (Postgres Pool + In-Memory массивы), как реализовано в `server.ts`.
4. Секреты (API ключи) бери только из `.env` или пример '.env.example'.

---

## 🚀 Текущий спринт: Парсинг смет и генерация документов

### Шаг 1: Очистка проекта от неиспользуемых ассетов (Рефакторинг)
- [x] Найти физические файлы векторов 1, 2, 4, 5, 6, 7 (например, `vector6.png`, `vector7.png` и другие) и удалить их из файловой системы проекта. Вектор 3 (и другие нужные) — оставить.
- [x] Найти во всех React-компонентах (проверить `App.tsx`, `HomeView.tsx` и другие View-компоненты) ссылки, импорты и теги `<img>`, которые ссылались на эти удаленные векторы, и аккуратно вычистить их из кода.
- [x] Убедиться, что после удаления приложение успешно собирается и запускается без ошибок об отсутствии файлов, а оставшаяся верстка не сломалась.

### Шаг 2: Обработка Excel файлов (Backend)
- [x] Настроить multer (или аналог) в `server.ts` для приема загружаемых пользователем Excel-файлов (.xlsx, .xls).
- [x] Написать утилиту с использованием библиотеки `xlsx` для чтения загруженного файла и его очистки (удаление пустых строк/столбцов, конвертация в плоский массив или легкий CSV/JSON для экономии токенов).

### Шаг 3: AI-анализ сметы (AI Engine / Backend)
- [x] Создать функцию обращения к Gemini API (`@google/genai`).
- [x] Написать сложный системный промпт для Gemini для обработки плоского массива из Excel. Правила парсинга для ИИ:
      1) Идентифицировать строительные работы (ориентир — глаголы или отглагольные существительные: монтаж, прокладка, установка). 
      2) Находить привязанные к работе материалы. Если материал указан в строках ниже — привязать его к работе. Если дополнительных строк нет — вычленить название материала из самого наименования работы.
      3) Извлекать "Объем" и "Единицу измерения".
      4) Исключить мусор: СТРОГО игнорировать и не добавлять в результат работы, у которых объем равен 0.
- [x] Настроить Structured Outputs в Gemini, чтобы ИИ возвращал строгий JSON массив объектов: `{ "workName": string, "materials": string, "quantity": number, "unit": string }` (где materials — это строка с перечислением материалов).
- [x] Создать API-эндпоинт `/api/estimates/parse`, который свяжет Шаг 2 и Шаг 3 (принимает распарсенный массив Excel, отправляет в ИИ, возвращает чистый JSON).

### Шаг 4: Архитектура и рефакторинг Личного кабинета (B2B SaaS Dashboard)
- [ ] **Багфикс и Стабилизация:** Устранить критическую ошибку парсинга `SyntaxError: Unexpected token 'u'` в `src/hooks/useProjects.ts`. Внедрить строгие проверки на `undefined` и безопасные fallback-состояния для локальных данных.
- [ ] **SaaS Layout & Навигация:** Разработать модульный Layout с боковым меню (Sidebar) на Tailwind CSS. Разделы: "Дашборд", "Объекты", "Журнал актов", "Справочники" и "Настройки". 
- [ ] **Сущность "Справочники" (Dictionaries):** Спроектировать UI для занесения реквизитов компаний (Заказчик, Подрядчик, Проектировщик) и ФИО подписантов, чтобы переиспользовать их при генерации.
- [ ] **Ролевая модель (RBAC) - UI каркас:** Заложить в интерфейс поддержку ролей (Администратор / Инженер ПТО) для будущего разделения прав доступа.
- [ ] **UX/UI Паттерны:** Внедрить Error Boundaries, Empty States (заглушки для пустых объектов) и скелетоны загрузки.

### Шаг 5: Загрузка смет и Интерактивная таблица (Frontend)
- [ ] Обновить интерфейс внутри конкретного "Объекта": добавить Drag&Drop зону для загрузки Excel-сметы.
- [ ] Связать фронтенд с уже готовым эндпоинтом `/api/estimates/parse` и добавить индикатор загрузки (запрос к Gemini занимает время).
- [ ] Установить библиотеку для таблиц (`@tanstack/react-table` или `react-data-grid`).
- [ ] Создать компонент `EditableActTable.tsx`, который рендерит ответ от ИИ. Реализовать "Inline Editing" (редактирование в ячейках: наименование работы, материалы, объем, ед. изм.).
- [ ] Добавить интерфейс выбора подписантов из "Справочников" для привязки к текущему акту.
- [ ] Добавить кнопку "Подтвердить данные и сгенерировать".

### Шаг 6: Генерация документов DOCX и PDF (Backend)
- [ ] Обновить логику эндпоинта `/api/documents/generate`: он должен принимать отредактированный JSON из таблицы и выбранные данные из Справочников.
- [ ] Имплементировать генерацию `.docx` с помощью `docxtemplater` + `pizzip` на основе шаблона `aosr_template.docx`.
- [ ] Добавить функционал конвертации сгенерированного `.docx` в `.pdf` (используя пакеты вроде `libreoffice-convert` или API-сервисы).
- [ ] Настроить отдачу файлов (или ссылок на них) на фронтенд для скачивания пользователем.

### Шаг 7: Сохранение прогресса и Реестр актов (База данных)
- [ ] Настроить сохранение сгенерированных актов в базу данных (в привязке к конкретному "Объекту"). Обязательно поддержать двойную логику: Postgres Pool + in-memory массивы.
- [ ] Вывести сохраненные акты в раздел "Журнал актов" (единый реестр) в виде удобной таблицы со статусами ("Черновик", "Сгенерирован", "Подписан").
- [ ] Реализовать возможность скачать ранее сгенерированный акт из Журнала без повторной траты токенов ИИ.

---

## 🐛 Баги и исправления (Backlog)
- [ ] *(Jules, записывай сюда найденные ошибки, которые не блокируют текущую задачу, чтобы вернуться к ним позже)*

---

## 💡 Идеи на будущее (Icebox)
- [ ] Поддержка файлов формата `.xml`(Гранд-Смета) и '.pdf'.
- [ ] Дать пользователю возможность скачивать файлы в .docx по отдельности или же собрать их все вместе в один файл .pdf .
- [ ] Автоматическое формирование актов входного контроля. Общего журнала работ (ОЖР) и журнала КС-6а на основе тех же данных.
- [ ] Гостевой доступ для Прораба (с мобильного телефона), чтобы он мог прикреплять фото скрытых работ к черновику акта.