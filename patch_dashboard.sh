sed -i "1s/^/import { ActCreateForm } from '..\/Acts\/ActCreateForm';\n/" src/components/views/Dashboard/ObjectDetailView.tsx
sed -i "s/const \[statusFilter, setStatusFilter\] = useState('all');/const [statusFilter, setStatusFilter] = useState('all');\n  const [isCreatingAct, setIsCreatingAct] = useState(false);/" src/components/views/Dashboard/ObjectDetailView.tsx
sed -i '/<div className="flex justify-center mt-4 w-full">/,/<\/div>/c\                <div className="flex justify-center mt-4 w-full">\n                  <button\n                    onClick={() => setIsCreatingAct(true)}\n                    className="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-blue-700 transition-colors shadow-sm whitespace-nowrap"\n                  >\n                    + Создать акт вручную\n                  <\/button>\n                <\/div>' src/components/views/Dashboard/ObjectDetailView.tsx
sed -i '/<div className="flex items-center gap-2 w-full sm:w-auto">/,/<\/div>\n              <button/c\              <div className="flex items-center gap-2 w-full sm:w-auto">\n                <Filter className="w-4 h-4 text-gray-500" />\n                <select\n                  value={statusFilter}\n                  onChange={(e) => setStatusFilter(e.target.value)}\n                  className="border border-gray-300 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-blue-500 outline-none w-full sm:w-auto bg-white"\n                >\n                  <option value="all">Все статусы</option>\n                  <option value="draft">Черновик (В работе)</option>\n                  <option value="review">На проверке</option>\n                  <option value="signed">Подписано</option>\n                  <option value="rejected">Отклонено</option>\n                </select>\n              </div>\n              <button\n                onClick={() => setIsCreatingAct(true)}\n                className="bg-blue-600 text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-blue-700 transition-colors shadow-sm whitespace-nowrap"\n              >\n                + Создать акт\n              </button>' src/components/views/Dashboard/ObjectDetailView.tsx
sed -i '/{activeTab === '\''estimates'\'' && (/a\          <>\n            {isCreatingAct ? (\n              <div className="bg-white rounded-xl border border-slate-200 p-6 shadow-sm">\n                <ActCreateForm \n                  onSubmit={(data) => {\n                    console.log('\''Act created:'\'', data);\n                    setIsCreatingAct(false);\n                    setToastMessage({ text: '\''Акт успешно создан'\'', type: '\''success'\'' });\n                  }} \n                  onCancel={() => setIsCreatingAct(false)} \n                />\n              </div>\n            ) : (parsedData || []).length === 0 ? (' src/components/views/Dashboard/ObjectDetailView.tsx
sed -i 's/(parsedData || \[\]).length === 0 ? (/ (/' src/components/views/Dashboard/ObjectDetailView.tsx
sed -i '/<\/div>\n        )}/i\          </>' src/components/views/Dashboard/ObjectDetailView.tsx
